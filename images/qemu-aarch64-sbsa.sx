;; Imported by silt import from configs/qemu_aarch64_sbsa_defconfig.
;; silt import --kbuild checks this composes back to the source exactly.

(image qemu-aarch64-sbsa
  (compose
    target:qemu-aarch64-sbsa
    profile:qemu-aarch64-sbsa)
  (verified-against (buildroot "2025.02.16")))
