# silt

A solver-driven reproducible Linux build system using S-expressions, SMT, deterministic DAG execution, and content-addressed artifacts.

Module: `github.com/vinodhalaharvi/silt`

Scaffolded by tilegen. Files marked `DO NOT EDIT` are regenerated from the spec;
everything else, including this README, is yours.

## Develop

```sh
make tidy check
```

Open implementation tasks for an LLM are listed in `tilegen.tasks.json`.
