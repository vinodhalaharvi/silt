[Unit]
Description=Silt CAN frames for the simulated device
After=silt-sim.service
Wants=silt-sim.service

[Service]
EnvironmentFile=-/etc/default/silt-can
ExecStart=/bin/sh -c 'exec /usr/bin/silt-cand -i "${IFACE:-vcan0}"'
Restart=on-failure

[Install]
WantedBy=multi-user.target
