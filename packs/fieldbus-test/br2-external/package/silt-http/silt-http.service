[Unit]
Description=Silt device config over HTTP on port 8080
After=silt-sim.service
Wants=silt-sim.service

[Service]
ExecStart=/usr/bin/silt-httpd
Restart=on-failure

[Install]
WantedBy=multi-user.target
